---
  name: "Hello Wold"
  on:
   push:
    branches:
      - master
      - production
  jobs:
    hello_world:
     runs-on: ubuntu-latest
     steps:
       - name: Print Hello World
         run: echo "Hello world"
       - name: Print Current Date
         run: date