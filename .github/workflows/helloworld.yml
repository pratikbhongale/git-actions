name: "hello world workflow"
on:
  push:
    branches:
      - master
      - production

    
jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: "Print HW"
        run: echo "Hello everyone"
